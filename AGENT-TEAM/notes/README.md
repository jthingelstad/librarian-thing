# notes/

Timestamped working notes, one file per run that has something worth recording:
`YYYY-MM-DD-<objective>-<topic>.md`. A note captures the evidence trail and the
decision, compactly — what was measured, what was concluded, what (if anything)
changed. Evidence about readers stays aggregate or anonymized; never paste
reader conversation content here. Healthy no-op runs need no note.
